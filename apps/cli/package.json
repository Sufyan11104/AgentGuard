{
  "name": "@agentguard/cli",
  "version": "0.1.0",
  "private": true,
  "description": "AgentGuard command-line interface.",
  "license": "MIT",
  "type": "module",
  "bin": {
    "agentguard": "./dist/index.js"
  },
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "lint": "eslint .",
    "test": "pnpm run build && vitest run tests --maxWorkers=1",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "dependencies": {
    "@agentguard/adapters": "workspace:*",
    "@agentguard/core": "workspace:*",
    "@agentguard/test-packs": "workspace:*",
    "commander": "^14.0.2",
    "yaml": "^2.8.2",
    "zod": "^4.1.13"
  },
  "devDependencies": {
    "@types/node": "^24.10.1"
  }
}
